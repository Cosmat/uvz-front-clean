# SPA fallback for Vue Router, BUT never serve HTML for missing assets
# (stale cached app.js referencing removed chunks must fail visibly, not return HTML as JS)

/assets/*  /404-asset.txt  404

# Vue Router history mode fallback
/*  /index.html  200
